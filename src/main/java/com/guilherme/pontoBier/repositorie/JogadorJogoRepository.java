package com.guilherme.pontoBier.repositorie;

import com.guilherme.pontoBier.model.JogadorJogo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface JogadorJogoRepository extends JpaRepository<JogadorJogo, Long> {
}
